#KVS_GLFW_DIR :=

GLFW_INCLUDE_PATH :=
GLFW_LIBRARY_PATH :=
GLFW_LINK_LIBRARY := -lglfw

ifdef KVS_GLFW_DIR
GLFW_INCLUDE_PATH := -I${KVS_EGL_DIR}/include
GLFW_LIBRARY_PATH := -L${KVS_EGL_DIR}/lib
else
#
#
endif

INCLUDE_PATH := -I../../../ ${GLFW_INCLUDE_PATH}
LIBRARY_PATH := -L../../Lib ${GLFW_LIBRARY_PATH}
LINK_LIBRARY := ${GLFW_LINK_LIBRARY} -lKVS.glfw
